<h2>プロフィール編集</h2>
<%= form_with model: @user, url: user_registration_path, method: :patch do |f| %>
  <div class="mt-1">自己紹介文を記載してください。</div>
  <div class="alert alert-info" role="alert">
    <%= f.text_area :comment, autofocus: true %>
    <%= f.submit "変更を保存" %>
  </div>
  <div class="mt-5">退会希望の場合は、退会ボタンを押してください。</div>
  <%= link_to "退会", user_registration_path, method: :delete, "data-confirm" => "本当に退会しますか？", class: "btn btn-danger btn-sm" %>
<% end %>
